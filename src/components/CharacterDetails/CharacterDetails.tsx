function CharacterDetails() {
  //const id = 1; //get the value from query param

  //const [films] = useState(() => id); // @todo get the values from api and filer them
  //const [starship] = useState(() => id); // @todo get the values from api and filer them

  return (
    <div className="p-4 space-y-6">
      <button>← Back to Character List</button>

      <div>Character Details</div>
      <div>Character Files</div>
      <div>Character startships</div>
    </div>
  );
}

export default CharacterDetails;
